# UnityBridge.Api.Sdk

UnityBridge.Api.Sdk 是一个专门用于输出的SDK转换项目。

```bash
# 编译像谬
dotnet run --project UnityBridge.Api.Sdk/UnityBridge.Api.Sdk.csproj

# 生成 SDK
# 会在一个client文件夹下生成各种编程语言strk。
generate-sdk-clients.sh
```